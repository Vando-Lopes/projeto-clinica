
import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    appBar: {
        position: 'relative',
        background: '#add8e6',
        height: '50px'
    },
}))